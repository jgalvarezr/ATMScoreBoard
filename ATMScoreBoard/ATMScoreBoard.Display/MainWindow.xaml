<Window x:Class="ATMScoreBoard.Display.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ATMScoreBoard.Display"
        xmlns:views="clr-namespace:ATMScoreBoard.Display.Views"
        xmlns:converters="clr-namespace:ATMScoreBoard.Display.Converters"
        mc:Ignorable="d"
        Title="ATM ScoreBoard" Height="1080" Width="1920"
        Background="#1E1E1E" Foreground="White"
        WindowState="Maximized" WindowStyle="SingleBorderWindow" FontFamily="./Fonts/#Poppins">

    <Grid>
        <Grid.Resources>
            <converters:BooleanToVisibilityConverter x:Key="BoolToVis" />
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVis" />
        </Grid.Resources>

        <Border Visibility="{Binding HayPartidaEnCurso, Converter={StaticResource BoolToVis}}" >
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*" />
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="4*" />
                </Grid.ColumnDefinitions>

                <views:TeamPanel Grid.Column="0" DataContext="{Binding EquipoA}" BorderColor="Cyan"/>


                <Grid Grid.Column="1"  Margin="20,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <views:StatBox Grid.Row="0" Margin="0,15,0,15" Title="RANKING" ValueA="{Binding RankPosicionA}" ValueB="{Binding RankPosicionB}"/>
                    <views:StatBox Grid.Row="1" Margin="0,15"      Title="PUNTOS"  ValueA="{Binding RankPuntosA}" ValueB="{Binding RankPuntosB}"/>
                    <views:StatBox Grid.Row="2" Margin="0,15"      Title="GLOBAL"  ValueA="{Binding VictoriasGlobalesA}" ValueB="{Binding VictoriasGlobalesB}"/>
                    <views:StatBox Grid.Row="3" Margin="0,15,0,15" Title="GAMES"   ValueA="{Binding VictoriasH2HA}" ValueB="{Binding VictoriasH2HB}"/>

                </Grid>                    
                

                <views:TeamPanel Grid.Column="2" DataContext="{Binding EquipoB}" BorderColor="Cyan"/>
            </Grid>
        </Border>

        <Border Visibility="{Binding HayPartidaEnCurso, Converter={StaticResource InverseBoolToVis}}">
            <TextBlock Text="Aquí se mostrarán los Rankings" FontSize="50" HorizontalAlignment="Center" VerticalAlignment="Center" TextWrapping="Wrap"/>
        </Border>
    </Grid>
</Window>