<UserControl x:Class="ATMScoreBoard.Display.Views.RankingJugadoresView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ATMScoreBoard.Display.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">


    <Grid>
        <ListView ItemsSource="{Binding}" 
          Background="Transparent" 
          BorderThickness="0" 
          Foreground="White"
          HorizontalContentAlignment="Stretch"
          Margin="50,10,50,10">

            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Cada fila es un Grid con 4 columnas proporcionales -->
                    <Grid Margin="0,5">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1*" />
                            <!-- Posición -->
                            <ColumnDefinition Width="4*" />
                            <!-- Nombre -->
                            <ColumnDefinition Width="1*"/>
                            <!-- Puntos -->
                        </Grid.ColumnDefinitions>

                        <!-- Columna 1: Posición -->
                        <Viewbox Grid.Column="0">
                            <TextBlock Text="{Binding PosicionRanking}" FontWeight="SemiBold"  FontSize="40" Foreground="DarkCyan" Margin="0,0,20,0"/>
                        </Viewbox>

                        <!-- Columna 2: Jugador A -->
                        <Viewbox Grid.Column="1" HorizontalAlignment="Left">
                            <TextBlock Text="{Binding Nombre}"  Foreground="LightGreen" FontSize="40" Margin="20,0"/>
                        </Viewbox>

                        <!-- Columna 3: Jugador B -->
                        <Viewbox Grid.Column="2">
                            <TextBlock Text="{Binding PuntosRanking}" FontWeight="SemiBold" Foreground="Cyan" HorizontalAlignment="Right"/>
                        </Viewbox>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>

            <!-- Hacemos que los contenedores de los items se estiren para ocupar toda la altura -->
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

        </ListView>
    </Grid>
</UserControl>